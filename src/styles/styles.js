import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#a295f5',
  },

  box: {
    height: 250,
    padding: 20,
    alignItems: 'center',
    justifyContent: 'center',
  },

  text: {
    color: "white",
    fontSize: 15,
    fontWeight: "bold",
  }
});

